# Quick Who Is the Undercover and manager confirmation

Date: 2026-09-01

## Authority

The user confirmed that the current `games/undercover.md` is the authoritative single Who Is the Undercover game. The user intentionally removed `games/undercover-notool.md`, retained only one quick mode, and optimized its prompt. The deletion and current prompt are desired content, not migration damage.

The user also confirmed retaining all presented Agent Games product behaviors: model-facing catalog CRUD, the Plugins settings game manager, Markdown-backed game storage, and deployed dependency-status hints.

## Consequence

The owning STATE must preserve a single quick Who Is the Undercover entry, on-demand prompt admission, the shared catalog authority used by model and browser surfaces, user-editable Markdown content, and advisory dependency display. Future maintenance must not restore a second historical variant merely because Git history contains one.
